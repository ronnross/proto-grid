(function() {
  let gridParents = document.querySelectorAll(".grid");
  console.log(gridParents);
  gridParents.forEach(el => {
    // el.classList.add("red")
    console.log('chilren', el.children)
  })  


})();
